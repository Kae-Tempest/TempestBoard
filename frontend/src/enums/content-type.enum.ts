export enum ContentType {
    applicationJson = "application/json",
    applicationXml = "application/xml",
    applicationTextPlain = "text/plain",
    applicationMultipartFormData = "multipart/form-data",
}